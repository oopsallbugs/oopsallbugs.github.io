import styles from "./Footer.module.css";

const Footer = () => (
  <footer className={styles.footer}>
    <p className={styles.footerText}>&copy; 2025 Sam Humphries</p>
  </footer>
);

export default Footer;
